---
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogCard from '../components/BlogCard.astro';
import SectionTitle from '../components/SectionTitle.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog'))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 3);
---

<BaseLayout title="ホーム">
  <!-- Hero Section -->
  <section class="relative bg-gradient-to-br from-navy-800 via-navy-700 to-navy-900 text-white overflow-hidden">
    <!-- Background pattern -->
    <div class="absolute inset-0 opacity-10">
      <div class="absolute top-0 right-0 w-96 h-96 bg-white rounded-full -translate-y-1/2 translate-x-1/3"></div>
      <div class="absolute bottom-0 left-0 w-64 h-64 bg-white rounded-full translate-y-1/3 -translate-x-1/4"></div>
    </div>

    <div class="container-wide relative py-20 sm:py-28 lg:py-36">
      <div class="max-w-3xl">
        <p class="text-accent-400 font-medium mb-3 text-sm sm:text-base tracking-wide">
          静岡県議会議員
        </p>
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-black mb-6 leading-tight">
          木内 満
          <span class="block text-2xl sm:text-3xl lg:text-4xl font-bold text-gray-300 mt-2">
            きうち みつる
          </span>
        </h1>
        <p class="text-lg sm:text-xl text-gray-300 mb-8 max-w-2xl leading-relaxed">
          富士宮から静岡を元気に。<br>
          県民の皆さまとともに、豊かで安心して暮らせる地域づくりに取り組んでまいります。
        </p>
        <div class="flex flex-wrap gap-4">
          <a href="/about/" class="btn-primary bg-accent-500 hover:bg-accent-600">
            プロフィールを見る
          </a>
          <a href="/blog/" class="btn-outline border-white text-white hover:bg-white hover:text-navy-800">
            活動報告を見る
          </a>
        </div>
      </div>
    </div>

    <!-- Wave separator -->
    <div class="absolute bottom-0 left-0 right-0">
      <svg viewBox="0 0 1440 60" fill="none" xmlns="http://www.w3.org/2000/svg" class="w-full">
        <path d="M0 60L48 55C96 50 192 40 288 35C384 30 480 30 576 33.3C672 36.7 768 43.3 864 45C960 46.7 1056 43.3 1152 38.3C1248 33.3 1344 26.7 1392 23.3L1440 20V60H0Z" fill="white"/>
      </svg>
    </div>
  </section>

  <!-- Profile Section -->
  <section class="py-16 sm:py-24 bg-white">
    <div class="container-wide">
      <SectionTitle subtitle="静岡県富士宮市選出の県議会議員として、地域のために活動しています">
        ごあいさつ
      </SectionTitle>

      <div class="max-w-4xl mx-auto">
        <div class="bg-gradient-to-r from-navy-50 to-gray-50 rounded-2xl p-8 sm:p-10">
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8 items-center">
            <div class="md:col-span-1 flex justify-center">
              <div class="w-48 h-48 bg-navy-200 rounded-full flex items-center justify-center">
                <span class="text-navy-600 text-6xl font-bold">木</span>
              </div>
            </div>
            <div class="md:col-span-2">
              <p class="text-gray-700 leading-relaxed mb-4">
                静岡県富士宮市生まれ。富士宮の豊かな自然と文化を愛し、地域の発展に全力で取り組んでいます。
              </p>
              <p class="text-gray-700 leading-relaxed mb-6">
                県議会議員として、防災・減災対策、子育て支援、地域経済の活性化など、
                県民の皆さまの暮らしに直結する課題に向き合い、具体的な成果を積み重ねてまいります。
              </p>
              <a href="/about/" class="btn-outline text-sm">
                詳しいプロフィールへ →
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Policy Section -->
  <section class="py-16 sm:py-24 bg-gray-50">
    <div class="container-wide">
      <SectionTitle subtitle="県民の皆さまの暮らしを守るための3つの重点政策">
        重点政策
      </SectionTitle>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        <div class="bg-white rounded-xl p-8 shadow-sm border border-gray-100 text-center">
          <div class="w-16 h-16 bg-navy-50 rounded-xl flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-navy-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
            </svg>
          </div>
          <h3 class="text-lg font-bold text-navy-800 mb-3">防災・減災</h3>
          <p class="text-sm text-gray-600 leading-relaxed">
            富士山噴火・地震・風水害への備えを強化。命を守る県土づくりを推進します。
          </p>
        </div>

        <div class="bg-white rounded-xl p-8 shadow-sm border border-gray-100 text-center">
          <div class="w-16 h-16 bg-navy-50 rounded-xl flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-navy-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.5 2.5 0 11-5 0 2.5 2.5 0 015 0z" />
            </svg>
          </div>
          <h3 class="text-lg font-bold text-navy-800 mb-3">子育て・教育</h3>
          <p class="text-sm text-gray-600 leading-relaxed">
            安心して子どもを産み育てられる環境整備。教育の質の向上に取り組みます。
          </p>
        </div>

        <div class="bg-white rounded-xl p-8 shadow-sm border border-gray-100 text-center">
          <div class="w-16 h-16 bg-navy-50 rounded-xl flex items-center justify-center mx-auto mb-4">
            <svg class="w-8 h-8 text-navy-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7h8m0 0v8m0-8l-8 8-4-4-6 6" />
            </svg>
          </div>
          <h3 class="text-lg font-bold text-navy-800 mb-3">地域経済</h3>
          <p class="text-sm text-gray-600 leading-relaxed">
            観光・農業・中小企業の振興で地域を活性化。雇用と活力のある静岡を目指します。
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Latest Blog Posts -->
  <section class="py-16 sm:py-24 bg-white">
    <div class="container-wide">
      <SectionTitle subtitle="県政活動や地域での取り組みをお伝えします">
        活動報告
      </SectionTitle>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {posts.map((post) => (
          <BlogCard
            title={post.data.title}
            description={post.data.description}
            pubDate={post.data.pubDate}
            category={post.data.category}
            slug={post.id}
          />
        ))}
      </div>

      <div class="text-center mt-10">
        <a href="/blog/" class="btn-outline">
          すべての活動報告を見る →
        </a>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-16 sm:py-20 bg-gradient-to-r from-navy-700 to-navy-900 text-white">
    <div class="container-wide text-center">
      <h2 class="text-2xl sm:text-3xl font-bold mb-4">
        皆さまの声をお聞かせください
      </h2>
      <p class="text-gray-300 max-w-2xl mx-auto mb-8">
        地域の課題や県政に関するご意見・ご要望をお待ちしています。<br>
        お気軽にご連絡ください。
      </p>
    </div>
  </section>
</BaseLayout>
